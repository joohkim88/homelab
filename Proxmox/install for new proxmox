apt update ; apt dist-upgrade -y ; apt install curl htop cpufrequtils lm-sensors fish vim needrestart -y ; apt autoremove -y ; usermod -s /usr/bin/fish root

mkdir /mnt/bindmounts ; mkdir /mnt/bindmounts/media ; mkdir /mnt/bindmounts/plex ; mkdir /mnt/bindmounts/vault

lvextend -l +100%FREE pve/data

nano /etc/default/cpufrequtils
GOVERNOR="powersave"

bash <(curl -Ss https://my-netdata.io/kickstart.sh) all

nano /usr/share/netdata/web/monitor.html
chown netdata:netdata /usr/share/netdata/web/monitor.html
